const blockForDigits = document.querySelector('.enter__digit');
createForm()
const calcButton = document.querySelector('.digit__button');
calcButton.addEventListener('click', () =>  getDistance(getValues()))
const r = 6371;
